import 'package:flutter/material.dart';

class ProfilePrometedScreen extends StatelessWidget {
  const ProfilePrometedScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
